apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.controller.service }}
spec:
  selector:
    app: orion
    name: controller
  ports:
  - protocol: TCP
    port: {{ .Values.controller.port | atoi }}
    targetPort: controller